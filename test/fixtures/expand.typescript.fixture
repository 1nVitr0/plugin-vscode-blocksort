class Test {
  expandSelection(input: number) {
    switch(input) {
      case 0:
        return {
          some: {
            fairly: {
              nested: 'object',
              with: 'many'
            },
            properties: 'and',
            sub: 'properties'
          },
          and: 'stuff',
        }
      default:
        return null;
    }
  }
}

class Test2 {
  public test: string;

  public constructor(test: string) {
    this.test = test;
  }
}
